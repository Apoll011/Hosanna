/// Shell branch indices for the [StatefulShellRoute.indexedStack] declared in
/// router.dart.
///
/// A branch's index equals its position in the `branches` list, and the shell's
/// navigation destinations (Floating Nav Bar) plus the sidebar/drawer rows must
/// follow the same order.
const int kSongsBranch = 0;
const int kServicesBranch = 1;
const int kMetronomeBranch = 2;
const int kCircleOfFifthsBranch = 3;
const int kSettingsBranch = 4;
